import React from "react";
import "./tile.css"

function Tile(props: {id: number}) {
        return (
            <div className="tile">
                {/*<span>{props.id}</span>*/}
            </div>
        )
}

export default Tile